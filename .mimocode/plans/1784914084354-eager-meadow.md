# План: PPTX поддержка + смена модели на DeepSeek v4-flash

## Цель
1. Добавить поддержку загрузки и обработки презентаций (.pptx)
2. Сменить модель с `deepseek-chat` на `deepseek-v4-flash`

## Изменения

### 1. `backend/requirements.txt`
Добавить зависимость:
```
python-pptx>=1.0.0
```

### 2. `backend/config.py`
Изменить дефолт модели:
```python
DEEPSEEK_MODEL: str = "deepseek-v4-flash"
```

### 3. `backend/services/llm.py`
Добавить импорт `pptx` и функцию `read_pptx()`:
```python
from pptx import Presentation

def read_pptx(file_content: bytes) -> str:
    prs = Presentation(io.BytesIO(file_content))
    texts = []
    for slide in prs.slides:
        for shape in slide.shapes:
            if shape.has_text_frame:
                for paragraph in shape.text_frame.paragraphs:
                    text = paragraph.text.strip()
                    if text:
                        texts.append(text)
    return "\n".join(texts)
```

### 4. `backend/routes/api.py`
В эндпоинте `upload_lectures` — определять тип файла по расширению и вызывать нужную функцию:
```python
from services.llm import read_docx, read_pptx, create_docx, process_text

# В цикле загрузки файлов:
if file.filename.lower().endswith('.pptx'):
    original_text = read_pptx(content)
else:
    original_text = read_docx(content)
```

### 5. `frontend/src/pages/ProjectPage.jsx`
- Строка 158: `accept=".docx"` → `accept=".docx,.pptx"`
- Строка 277: текст "Загрузите DOCX файлы" → "Загрузите DOCX или PPTX файлы"

### 6. `.env.example`
Обновить комментарий и дефолт:
```
DEEPSEEK_MODEL=deepseek-v4-flash
```

## Файлы для изменения (6 штук)
| Файл | Изменение |
|------|-----------|
| `backend/requirements.txt` | +`python-pptx` |
| `backend/config.py` | дефолт модели |
| `backend/services/llm.py` | +`read_pptx()`, импорт |
| `backend/routes/api.py` | роутинг по типу файла |
| `frontend/src/pages/ProjectPage.jsx` | accept + текст |
| `.env.example` | дефолт модели |

## Верификация
1. `pip install python-pptx` в venv
2. Запустить backend, загрузить .pptx файл → текст извлекается
3. Загрузить .docx файл → работает как раньше
4. Проверить что API вызывает `deepseek-v4-flash` (логи или ответ)
5. Frontend: сборка `npm run build` без ошибок
